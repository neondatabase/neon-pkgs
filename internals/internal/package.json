{
	"name": "@neon-internals/internal",
	"version": "0.0.0",
	"private": true,
	"description": "Client runtime/framework detection and X-Neon-Client-Info header injection shared by @neon/auth and @neon/postgrest-js. Never published - bundled into each consumer.",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc --noEmit && tsdown",
		"prepare": "npm run build"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"tsdown": "catalog:",
		"typescript": "catalog:"
	},
	"engines": {
		"node": ">=20.19.0"
	}
}
