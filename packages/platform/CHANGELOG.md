# @neondatabase/platform

## 0.1.0

### Minor Changes

- Initial release. Adds `defineConfig`, `pullConfig`, and `pushConfig` for managing the Neon Platform as code via a `neon.ts` config file. Exported under `@neondatabase/platform` and `@neondatabase/platform/v1`.
