---
"neon-init": patch
---

Allow skill installs from neon init to be reported to skills.sh , so installations count as executions on the skills ecosystem. User's own DISABLE_TELEMETRY env is still respected.
