---
"neon": minor
---

Add `neon logs query`, `neon logs fields` and `neon logs field-values <field>` for reading the logs a branch's functions, object storage and Postgres computes emit. Requires Neon Platform Beta, and is currently available only for projects in `aws-us-east-2`.
