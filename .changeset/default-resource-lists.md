---
"neon": minor
---

Allow `neon projects` and `neon branches` to list resources without an
explicit `list` subcommand. These bare commands no longer show group help; use
`--help` to see their subcommands.
