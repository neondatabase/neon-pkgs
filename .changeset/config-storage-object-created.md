---
"@neon/config": minor
"@neon/config-runtime": minor
"neon": minor
---

Declare `storage_object_created` Function triggers in `neon.ts`, apply them with `neon deploy`, and create them with `neon triggers create --bucket`.
