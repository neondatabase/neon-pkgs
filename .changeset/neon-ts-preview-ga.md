---
"@neon/config": minor
"@neon/env": minor
"@neon/config-runtime": minor
"neon": minor
---

AI Gateway, Functions, and Object Storage in neon.ts are GA as top-level `aiGateway`, `functions`, and `buckets`. The same keys still work under `preview` (deprecated); `neon config apply` / `deploy` warn that they can be lifted.
