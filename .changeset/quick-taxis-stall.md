---
"neon": minor
---

Add `neon inspect db stalled-queries`, a read-only snapshot of active queries running longer than 30 seconds with parallel-worker grouping, waits, blockers, query IDs, and timestamps.
