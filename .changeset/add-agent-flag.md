---
"neon-init": minor
---

Add `--agent` / `-a` flag to configure a single agent without the editor selection prompt. Accepts `cursor`, `copilot`, or `claude` (and common aliases like `Cursor`, `VS Code`, `Claude CLI`).
