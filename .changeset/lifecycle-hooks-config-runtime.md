---
"@neon/config-runtime": minor
---

Add `runHook` and `runShellHook` to execute the lifecycle hooks declared under `experimental.hooks` in `neon.ts` (function or shell-command form), plus the `HookExecutionError` class and `RunHookOptions` type.
