---
"@neon/sdk": minor
"@neon/tools": minor
---

Wrap Function triggers (`neon.functions.triggers`) and credential reveal/rotate. Create input scopes stay `CredentialScope`; responses now type `GrantedCredentialScope[]`. Branch delete no longer accepts `hard_delete`.
