---
"neon": minor
"neonctl": minor
---

Add `neon mcp` to install the Neon MCP server into coding agents. Default mints an API key; `--oauth` skips login. A TTY picker selects agents (`--agent` or detected agents in CI).
