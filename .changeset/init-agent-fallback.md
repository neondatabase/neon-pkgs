---
"neon": minor
"neonctl": minor
---

`neon init -y` / `neon bootstrap --default`, `neon skills -y`, and `neon plugins -y` install agent tooling into project folders, the host CLI agent, or installed apps. Init skips agent setup if none are detected; skills and plugins still require `--agent` when none are.
