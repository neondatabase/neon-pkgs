---
"neon": minor
"neonctl": minor
---

Add `neon plugins` to install the Neon agent plugin into coding agents. A TTY asks agents, then confirms. `-y` installs into detected agents, project-scoped. `--agent` names specific agents. `--global` is user-level.
