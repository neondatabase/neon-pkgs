---
"@neon/postgrest-js": minor
---

Add `@neon/postgrest-js`, migrated from `neondatabase/neon-js` (previously `@neondatabase/postgrest-js`) — a generic PostgreSQL client for the Neon Data API with no authentication dependency. For auth-integrated clients, pair it with `@neon/auth` or bring your own token provider.
