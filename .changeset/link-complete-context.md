---
"neon": major
---

`neon link` always writes a complete `.neon` (organization when the project has one, project, and branch). Org-only, unpinned, and `--no-checks` writes without `--branch` now fail.
